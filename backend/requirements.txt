# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Validation
pydantic==2.9.2
python-dotenv==1.0.1

# AWS — single SDK for Bedrock, Cognito, DynamoDB
boto3==1.35.36
botocore==1.35.36

# JWT verification for Cognito tokens
PyJWT[crypto]==2.9.0
cryptography==43.0.1

# Anti-hallucination: two-call knowledge-declaration architecture
# No external search APIs required — all grounding via prompt engineering
